steps:
1. clone the repo
2. before that configure aws. use the existing keypair in aws and check that the default security group allows ssh. 
3. run terraform init
4. terraform plan
5. terraform apply
6. login to master
7. which ansible
8. ls
9. check hosts.cfg file
10.  ansible demo -m ping -i hosts.cfg   -> if poped for fingerprint connect enter yes
11.  
